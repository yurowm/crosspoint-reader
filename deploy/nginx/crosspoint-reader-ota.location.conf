location ^~ /crosspoint-reader/ {
    alias /var/www/crosspoint-reader/;
    try_files $uri =404;
    default_type application/octet-stream;
    types {
        application/json json;
    }
    add_header Cache-Control "no-cache" always;
}
