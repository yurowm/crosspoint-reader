diff --git a/libs/network/SecureNet/include/SecureHttpClient.h b/libs/network/SecureNet/include/SecureHttpClient.h
index ceaa3ba..eb34719 100644
--- a/libs/network/SecureNet/include/SecureHttpClient.h
+++ b/libs/network/SecureNet/include/SecureHttpClient.h
@@ -38,6 +38,8 @@
 #include <algorithm>
 #include <cctype>
 #include <iterator>
+#include <memory>
+#include <new>
 #include <cstdint>
 #include <cstdlib>
 #include <functional>
@@ -502,12 +504,17 @@ class SecureHttpClient {
 
   // Streams exactly `count` body bytes.
   bool readFixed(Client& c, size_t count, const DataCallback& onData, const AbortCallback& shouldAbort = nullptr) {
+#if FREEINK_DEVICE_X4PRO
+    uint8_t* buf = bodyReadBuffer();
+    if (!buf) return false;
+#else
     uint8_t buf[READ_CHUNK];
+#endif
     size_t remaining = count;
     unsigned long deadline = millis() + _timeoutMs;
     while (remaining > 0) {
       if (isAborted(shouldAbort)) return false;
-      const size_t want = remaining < sizeof(buf) ? remaining : sizeof(buf);
+      const size_t want = remaining < READ_CHUNK ? remaining : READ_CHUNK;
       const int n = c.read(buf, want);
       if (n <= 0) {
         if (!c.connected() && c.available() == 0) return false;
@@ -524,11 +531,16 @@ class SecureHttpClient {
 
   // Streams body bytes until the peer closes (Connection: close, no length).
   bool readUntilClose(Client& c, const DataCallback& onData, const AbortCallback& shouldAbort = nullptr) {
+#if FREEINK_DEVICE_X4PRO
+    uint8_t* buf = bodyReadBuffer();
+    if (!buf) return false;
+#else
     uint8_t buf[READ_CHUNK];
+#endif
     unsigned long deadline = millis() + _timeoutMs;
     for (;;) {
       if (isAborted(shouldAbort)) return false;
-      const int n = c.read(buf, sizeof(buf));
+      const int n = c.read(buf, READ_CHUNK);
       if (n <= 0) {
         if (!c.connected() && c.available() == 0) return true;
         if (static_cast<int32_t>(millis() - deadline) >= 0) return false;
@@ -561,12 +573,18 @@ class SecureHttpClient {
     }
   }
 
-  // Body read buffer. 2 KB drains wolfSSL's decrypted TLS records in few
-  // enough read() calls to keep large downloads moving: at 512 B a consuming
-  // firmware measured ~30 KB/s and slow CDNs (Cloudflare) dropped the
-  // connection mid-stream; 2 KB removed the stall. Stack-allocated in the
-  // body readers, so kept modest.
+  // X4 Pro has enough heap for full-size TLS records and an 8 KB transfer
+  // buffer. Low-memory targets retain the proven 2 KB stack buffer.
+#if FREEINK_DEVICE_X4PRO
+  static constexpr size_t READ_CHUNK = 8192;
+  uint8_t* bodyReadBuffer() {
+    if (!_bodyReadBuffer) _bodyReadBuffer.reset(new (std::nothrow) uint8_t[READ_CHUNK]);
+    return _bodyReadBuffer.get();
+  }
+  std::unique_ptr<uint8_t[]> _bodyReadBuffer;
+#else
   static constexpr size_t READ_CHUNK = 2048;
+#endif
   static constexpr size_t MAX_LINE = 4096;  // header / chunk-size line cap
 
   // Kept-alive connection state. _conn points at _secure or _plain while a
diff --git a/libs/network/SecureNet/src/SecureClient.cpp b/libs/network/SecureNet/src/SecureClient.cpp
index 2546073..1e267f6 100644
--- a/libs/network/SecureNet/src/SecureClient.cpp
+++ b/libs/network/SecureNet/src/SecureClient.cpp
@@ -111,7 +111,7 @@ int SecureClient::connectWithMethod(const char* host, uint16_t port, void* metho
   // with a HelloRetryRequest and wolfSSL falls back to its group list.
   wolfSSL_UseKeyShare(ssl, WOLFSSL_ECC_X25519);
 #endif
-#ifdef HAVE_MAX_FRAGMENT
+#if defined(HAVE_MAX_FRAGMENT) && !FREEINK_DEVICE_X4PRO
   // Ask the peer to cap TLS records at 2KB (RFC 6066 max_fragment_length).
   // wolfSSL sizes its receive buffer to each incoming record, so a default
   // 16KB record demands a ~17KB contiguous allocation per record -- measured
